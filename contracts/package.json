{
  "name": "contracts",
  "private": true,
  "scripts": {
    "build": "forge build",
    "test": "forge test -vvv",
    "deploy:local": "forge script script/Deploy.s.sol:Deploy --rpc-url http://localhost:8545 --broadcast",
    "deploy:arc-testnet": "forge script script/Deploy.s.sol:Deploy --rpc-url $ARC_TESTNET_RPC_URL --broadcast"
  }
}
